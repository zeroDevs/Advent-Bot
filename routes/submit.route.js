const route = require("express").Router();

// THIS IS CURRENTLY LOCATED IN /web/authApi.js
// Eventually the submit section will migrate here

/**
 * This is the route responsible for handling entering the submission into the database.
 **/

route.get("/", (req, res) => {});
